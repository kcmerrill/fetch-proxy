defaults
        mode    http
        option forwardfor
        timeout connect 500
        timeout client __timeout__
        timeout server __timeout__

frontend fe_port_eighty
        bind *:80

__frontend__
__backend__
